{
  "name": "threadscoaching.co.uk",
  "version": "0.1.0",
  "description": "The Threads Coaching website.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "preview:cf": "npm run build && wrangler pages dev",
    "check": "astro check",
    "check:source": "node scripts/check-source-hygiene.mjs",
    "check:budget": "node scripts/check-output-budget.mjs",
    "check:build": "node scripts/check-build.mjs",
    "check:migrations": "node scripts/check-redirect-map.mjs",
    "check:redirects": "npm run check:migrations",
    "verify:preview": "npm run check:source && npm run check:migrations && npm run check && SITE_ENV=preview npm run build && SITE_ENV=preview npm run check:build && npm run check:budget",
    "verify:production": "npm run check:source && npm run check:migrations && npm run check && SITE_ENV=production npm run build && SITE_ENV=production npm run check:build && npm run check:budget"
  },
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.18.0 <25"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "astro": "^7.2.2",
    "typescript": "^6.0.3",
    "wrangler": "^4.123.0"
  }
}
